此扩展只能绘制图像的第一象限。
对于svg,图像存在二、三、四象限，这些象限不会被绘制出来。
请修复这个问题

另外，
```绘制纹理 [NAME] 到 x [X] y [Y] 宽 [W] 高 [H] 锚点 [ANCHOR] 层 [LAYER] 旋转 [ROT] 透明 [OP]```
需要单独分离。
分成
```
绘制纹理 [NAME] 于 [x] [y] [layer]

绘制当前纹理 [x] [y] [layer]

设置纹理[NAME] 的 旋转/锚点/不透明度/宽/高/横分辨率/纵分辨率为 xxx (设置属性后下一次绘制生效)

设置当前纹理 的 旋转/锚点/不透明度/宽/高/横分辨率/纵分辨率为 xxx (设置属性后下一次绘制生效)

重置纹理 [NAME] 的属性
```